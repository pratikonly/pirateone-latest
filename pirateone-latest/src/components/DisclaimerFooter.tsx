const DisclaimerFooter = () => {
  return (
    <footer className="relative z-10 py-4 px-6 text-center border-t border-border/50 bg-background/80 backdrop-blur-sm mt-8">
      <p className="text-xs text-muted-foreground">
        This site does not store any files on the server, we only linked to the media which is hosted on 3rd party services.
      </p>
    </footer>
  );
};

export default DisclaimerFooter;
